package com.preventia.domain.enums;

public enum UserRole {
    DOCTOR,
    SPONSOR,
    RECIPIENT,
    /** Field health coach — records vitals and performs pill counts at home visits */
    SAHAYAK,
    /** Pharmacy partner — receives prescriptions and submits fulfilment bids */
    PHARMACY
}
